fastapi==0.104.1
stripe==15.6.1
uvicorn[standard]==0.24.0
sqlmodel==0.0.22
requests==2.32.5
python-dotenv==1.0.1
# Postgres driver. Unused locally (SQLite is the fallback) but required
# wherever DATABASE_URL is set, which is every real deployment.
psycopg[binary]==3.2.3
